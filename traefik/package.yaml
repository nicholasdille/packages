name: traefik
description: The Cloud Native Application Proxy
repo: traefik/traefik
links:
- text: Homepage
  url: https://traefik.io/
version:
  latest: 2.3.4 # renovate: datasource=github-releases depName=traefik/traefik
  command: traefik version
  filter: ^Version
  pattern: s/^Version:\s+(.+)$/\1/
tags:
  - container
  - kubernetes
  - k8s
  - network
  - security
install:
  script: |-
    echo "https://github.com/traefik/traefik/releases/download/v${requested_version}/traefik_v${requested_version}_linux_amd64.tar.gz" | \
        download_file | \
        untar_file traefik
