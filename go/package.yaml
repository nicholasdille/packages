name: go
description: The Go programming language
repo: golang/go
version:
  latest: go1.15.4 # renovate: datasource=github-tags depName=golang/go versioning=regex:^go(?<major>\d+)\.(?<minor>\d+)\.(?<patch>\d+)$
  command: go version
  filter: ^go
  pattern: s/^go\sversion\s(\S+)\s.+$/\1/
tags:
- go
files:
- name: profile.d.go.sh
  content: |
    #!/bin/bash
    export PATH="${TARGET_BASE}/gvm/gos/${GO_VERSION}/bin:${PATH}"
