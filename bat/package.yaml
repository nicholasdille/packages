name: bat
description: A cat(1) clone with wings
repo: sharkdp/bat
version:
  latest: 0.17.1 # renovate: datasource=github-releases depName=sharkdp/bat
  command: bat --version
  filter: ^bat
  pattern: s/^bat\s(.+)$/\1/
tags:
  - console
install:
  script: |-
    echo "https://github.com/sharkdp/bat/releases/download/v${requested_version}/bat-v${requested_version}-i686-unknown-linux-gnu.tar.gz" | \
        download_file \
        >"${temporary_directory}/bat.tar.gz"

    ${SUDO} tar -xz -f "${temporary_directory}/bat.tar.gz" -C "${TARGET_BIN}" --wildcards --strip-components=1 "*/bat"

    mkdir -p "${TARGET_BASE}/man/man1"
    ${SUDO} tar -xz -f "${temporary_directory}/bat.tar.gz" -C "${TARGET_BASE}/man/man1" --wildcards --strip-components=1 "*/bat.1"
