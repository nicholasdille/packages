name: kubie
description: A more powerful alternative to kubectx and kubens
repo: sbstp/kubie
version:
  latest: v0.11.1 # renovate: datasource=github-releases depName=sbstp/kubie
  command: kubie --version
  filter: ^kubie
  pattern: s/^kubie\s(.+)$/v\1/
tags:
  - kubernetes
  - k8s
  - context
  - namespace
install:
  script: |-
    echo "https://github.com/sbstp/kubie/releases/download/${requested_version}/kubie-linux-amd64" | \
        download_file | \
        store_file kubie | \
        make_executable

    echo "https://github.com/sbstp/kubie/raw/${requested_version}/completion/kubie.bash" | \
        download_file | \
        store_completion kubie
