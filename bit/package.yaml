name: bit
description: Bit is a modern Git CLI
repo: chriswalz/bit
version:
  latest: v0.9.11 # renovate: datasource=github-releases depName=chriswalz/bit
  command: bit --version
  filter: ^bit
  pattern: s/^bit\sversion\s(.+)$/v\1/
tags:
  - pipeline
  - continuous
  - ci/cd
  - integration
  - delivery
  - deployment
  - container
install:
  script: |-
    echo "https://github.com/chriswalz/bit/releases/download/${requested_version}/bit_${requested_version#v}_linux_amd64.tar.gz" | \
        download_file | \
        untar_file bit
