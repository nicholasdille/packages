name: jid
description: json incremental digger
repo: simeji/jid
version:
  latest: v0.7.6 # renovate: datasource=github-releases depName=simeji/jid
  command: jid --version
  filter: ^jid
  pattern: s/^jid\sversion\s(.+)$/\1/
tags:
  - json
install:
  script: |-
    echo "https://github.com/simeji/jid/releases/download/${requested_version}/jid_linux_amd64.zip" | \
        download_file | \
        store_file "jid.zip" "${temporary_directory}"
    unzip_file "${temporary_directory}/jid.zip
